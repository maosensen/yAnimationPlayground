/**
 * Site-level facts that are not UI copy — external profiles linked from the
 * system header. Kept separate from components so a fork only edits one file.
 */
export const site = {
  links: {
    github: "https://github.com/maosensen",
    x: "https://x.com/catforestai",
  },
} as const;
