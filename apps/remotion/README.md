# Remotion workspace

Reserved for frame-driven React video compositions. Initialize Remotion here
only when the first experiment needs its renderer so the web playground stays
free of video-rendering dependencies.
