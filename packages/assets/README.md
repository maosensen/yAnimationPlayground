# Shared assets

Source-controlled SVG, font, image, audio, and video fixtures shared by labs
and render workspaces live here. Generated renders belong to their producing
workspace and are not shared assets.
