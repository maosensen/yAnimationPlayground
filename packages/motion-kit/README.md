# Motion kit

Reserved for reusable animation primitives with explicit, granular exports.
Technology-specific adapters stay separate so importing one primitive never
pulls every animation runtime into a consumer bundle.
