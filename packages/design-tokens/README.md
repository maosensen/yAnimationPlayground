# Design tokens

Future cross-runtime color, typography, spacing, radius, and elevation
contracts live here. During the monorepo migration, the complete working theme
system intentionally remains in `apps/web/src/app/globals.css` as the single
source of truth; tokens move here only after a second consumer proves the
shared contract.
